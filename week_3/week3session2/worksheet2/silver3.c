#include <stdio.h>
char str_length (char string)
{
    char answer;
    answer = len(string);
}

char main()
{
    char string = "Hello";
    printf("the length of the string is\n",str_length(string));
return 0;
}