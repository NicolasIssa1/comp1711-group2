#include <stdio.h>
int main() {
int number;
while(number <-1 || number >-1)
{
    printf("enter a number :");
    scanf("%d", &number);

if(number >=0 && number <=100 || number ==-1)
 {
     printf("program validated\n");
 }
else
{
    printf("program not validated\n");
}
 
return 0;
}

}